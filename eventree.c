#include <stdio.h>

// Skiena connectivity p 487 (pdf style)

int main() {
  int i, j, m, n;
  scanf("%d %d", &n, &m); // #vertices and #edges

  for (i = 0; i < m; i++) {
    // scan each line for integers ui vi
  }
